% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-mixed-effects-models.R,
%   R/create-mixed-effects-tbl.R
\name{fit_mixed_effects_models}
\alias{fit_mixed_effects_models}
\alias{create_mixed_effects_tbl}
\alias{print.mixed_effects_table}
\title{Fit Mixed-Effects Models}
\usage{
fit_mixed_effects_models(
  data,
  fixed = "TimePoint*SampleGroup",
  random = "~ 1 | pid"
)

create_mixed_effects_tbl(x)

\method{print}{mixed_effects_table}(x, n = 6L, ...)
}
\arguments{
\item{data}{The \code{data.frame} containing data as columns.}

\item{fixed}{\code{character(1)}. Indicating the right-hand-side of the
formula for the fixed effect.}

\item{random}{\code{character(1)}. Indicating the full formula for the random
effect. The default fits subject specific intercepts. Typically "group"
field indicator (e.g. subjects) are to the right side of the "|" and this
field \emph{must} be a column name in \code{data}.}

\item{x}{A list of mixed-effects models calculated
using \code{\link[=fit_mixed_effects_models]{fit_mixed_effects_models()}}. Alternatively, an object of
class \code{"mixed_effects_table"} for the S3 print method.}

\item{n}{\code{integer(1)}The number of rows to
show in the S3 print method.}

\item{...}{Additional arguments as required
by default S3 print methods.}
}
\value{
A list (invisibly) of fitted linear mixed-effects response models
(\code{class = lme}) with extra attributes attached for print methods.

A \code{mixed_effects_table} class object with the
following components:
\item{stat_table}{A data frame (table) of the results:
\describe{
\item{Time effect:}{Both F-values and p-values for TimePoint}
\item{Group effect:}{Both F-values and p-values for Group}
\item{p_value:}{Unadjusted p-values for the KS-distances}
\item{fdr:}{FDR adjustment to p-values}
\item{p_bonferroni:}{Bonferroni corrected p-value}
\item{rank:}{Ranking by the test significance.}
}
}
\item{test}{The name of the model fit, "Linear Mixed-effects Model".}
\item{call}{The direct call made the \code{\link[=fit_mixed_effects_models]{fit_mixed_effects_models()}}.}
\item{data_dim}{The dimensions of the data frame.}
\item{method}{The fitting method used by \code{lme}.}
\item{fixed}{The fixed-effects.}
\item{observations}{The number of total observations.}
\item{PIDfield}{The field name identifying the subjects in the data frame.}
\item{subjects}{The number of subjects.}
}
\description{
Fit an identical series of mixed-effects models according
for each feature in a data set.

The ordering of the table is determined by the fixed effects
argument passed to \code{\link[=fit_mixed_effects_models]{fit_mixed_effects_models()}}. Generally,
if there is an interaction term, that p-value is used, otherwise
the other fixed effects, usually \code{TimePoint}.
}
\details{
By default, random intercept models (subject specific offsets)
are fit with a fixed effect for slope: \code{random = ~ 1 | group}.
This means that group/subject specific slopes are not fit, and all
fits will be parallel with respect to slope. This may or may not
be what you intend but for most of proteomic data analysis
this is a reasonable default.
}
\section{Functions}{
\itemize{
\item \code{print(mixed_effects_table)}: S3 print method for \code{mixed_effects_table} class.

}}
\examples{
df <- data.frame(
  pid       = 1041:1080,
  Pop       = rep(utils::head(LETTERS, 10), 4),
  TimePoint = rep(c("baseline", "6 months", "12 months", "24 months"), each = 10),
  seq.1234.56 = stats::rnorm(40, mean = 25, sd = 3.5),
  seq.6969.4  = stats::rnorm(40, mean = 25, sd = 1.5),
  Response  = factor(sample(c("Control", "Disease"), 40, replace = TRUE))
)

df$TimePoint <- factor(df$TimePoint, levels = c("baseline", "6 months",
                                                "12 months", "24 months"))

models <- df[, c("TimePoint", "Response", "Pop", mixr:::get_analytes(df))] |>
  fit_mixed_effects_models(fixed = "TimePoint*Response", random = "~ 1 | Pop")

lapply(models, summary)

class(models)

lme_tab <- create_mixed_effects_tbl(models)
# S3 print method
lme_tab

}
\seealso{
\code{\link[=fit_lme_safely]{fit_lme_safely()}}
}
\author{
Stu Field
}
